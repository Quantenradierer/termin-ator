services:
  bot:
    build: .
    env_file: .env
    environment:
      SQLITE_PATH: /app/data/restaurants.db
    volumes:
      - bot-data:/app/data
    restart: unless-stopped

volumes:
  bot-data:
